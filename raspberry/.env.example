# ---------------------------------------------------------------------------
# DEVICE settings
# ---------------------------------------------------------------------------

# Comma-separated list of Azure IoT Hub connection strings (one per device)
DEVICE_CONNECTION_STRINGS=HostName=...;DeviceId=...;SharedAccessKey=...,HostName=...;DeviceId=...;SharedAccessKey=...

# Comma-separated list of device IDs
DEVICE_IDS=device-1,device-2

# Comma-separated number of valves per device (one int per device, same order as IDs)
DEVICE_N_VALVES=4,2

# Whether devices should receive incoming data (default: true)
DEVICE_RECEIVE_DATA=true

# ---------------------------------------------------------------------------
# CHIRPSTACK settings
# ---------------------------------------------------------------------------

# ChirpStack API key
CHIRPSTACK_API_KEY=

# ChirpStack server URL (e.g. http://localhost:8080)
CHIRPSTACK_SERVER_URL=

# ---------------------------------------------------------------------------
# INFRA settings (database, etc.)
# ---------------------------------------------------------------------------

# Path to the SQLite database file (relative to the working directory)
INFRA_DB_PATH=data/database.db

# ---------------------------------------------------------------------------
# IOT HUB SERVICE settings (for sending cloud-to-device messages)
# ---------------------------------------------------------------------------
# Get it from: Azure Portal → IoT Hub → Security settings → Shared access policies
# Use the "iothubowner" policy (or a custom policy with "Service connect" permission)
# Format: HostName=<hub>.azure-devices.net;SharedAccessKeyName=iothubowner;SharedAccessKey=<key>
IOTHUB_SERVICE_CONNECTION_STRING=HostName=<hub>.azure-devices.net;SharedAccessKeyName=iothubowner;SharedAccessKey=<key>